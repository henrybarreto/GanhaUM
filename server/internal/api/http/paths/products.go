package paths

const (
	CreateProduct string = "/products"
	GetProduct    string = "/products/:id"
	GetProducts   string = "/products"
	UpdateProduct string = "/products/:id"
	DeleteProduct string = "/products/:id"
)
